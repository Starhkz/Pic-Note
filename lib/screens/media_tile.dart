import 'package:flutter/material.dart';

class NoteMediaTile extends StatelessWidget {
  final bool isMedia;
  final String? note;
  final String? imageUrl;
  const NoteMediaTile(
      {Key? key, required this.isMedia, this.note, this.imageUrl})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
